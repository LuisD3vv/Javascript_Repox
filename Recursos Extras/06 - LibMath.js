// Objeto Math

//window.Math.Random(); muestra todas las funciones de la libreria, esta o cualquier otra

let resultado;

resultado = Math.PI;
resultado = Math.round(2.2); // redondea al mas cercano
resultado = Math.ceil(2.1); // redondeo hacia arriba
resultado = Math.floor(2.9); // redondeo hacia abajo
resultado = Math.sqrt(144); // raiz cuadrada
resultado = Math.abs(-122); //absoluto (sin signo)
resultado = Math.min(4,3,4,5,44); // valor minimo de un array
resultado = Math.max(4,3,4,5,44); // valor maximo de un array
resultado = Math.random(); // funcion aleatoria ente 0 y 1
// el numero es el limite es decir
const n = 10
resultado = Math.floor(Math.random() * n);
// ahora generara numeros entre 0 y 10.

console.log(resultado);