const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)); /// es una funcion por los parentesis
console.log(numero1.toString()); // este es un metodo por el punto (aunque tambien tenga parentesis)
